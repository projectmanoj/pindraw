[project]
name = "pydantic-models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "itsmanojthapa", email = "itsmanojthapa@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
pydantic-models = "pydantic_models:main"

[build-system]
requires = ["uv_build>=0.9.17,<0.10.0"]
build-backend = "uv_build"
